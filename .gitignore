node_modules
dist
dist-ssr
*.local
.DS_Store
*.log
video/

# signing material — never in history (the 2026-08-03 lesson)
*.keystore
*.jks
android/keystore.properties

# capture output — media never enters history
frames/
frames-*/
*.mp4
